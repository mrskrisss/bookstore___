// import CardPost from '../components/cardPost'

export const ListPosts = () => {
//     const renderPosts = () => {
//     if (posts.length === 0) return <div>Not Found</div>

//     if (isLoading) return <div>Loading...</div>

//     if (error) return <div className="alert alert-danger">{error}</div>

//     return posts.map(post => <CardPost key={post.id} id={post.id} title={post.title} text={post.body} image={post.image} likes={post.likes} dislikes={post.dislikes}/>)
//   }

  return (
    <ul className="wrapper-cards" >
      {/* {renderPosts()} */}
    </ul>
  )
}